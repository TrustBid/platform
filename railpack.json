{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "install": {
      "inputs": ["."],
      "commands": ["npm install"]
    },
    "build": {
      "inputs": ["..."],
      "commands": ["npm run build --workspace=apps/api"]
    }
  },
  "deploy": {
    "startCommand": "node apps/api/dist/main.js"
  }
}
